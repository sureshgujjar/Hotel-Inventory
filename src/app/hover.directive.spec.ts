import { HoverDirective } from './hover.directive';

describe('HoverDirective', () => {
  it('should create an instance', () => {
    // const directive = new HoverDirective();
    const directive =HoverDirective;
    expect(directive).toBeTruthy();
  });
});
